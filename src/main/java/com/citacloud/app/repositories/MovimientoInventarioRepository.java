package com.citacloud.app.repositories; import com.citacloud.app.models.MovimientoInventario; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface MovimientoInventarioRepository extends JpaRepository<MovimientoInventario,UUID>{List<MovimientoInventario> findByEmpresaIdOrderByCreadoEnDesc(UUID empresaId); long countByEmpresaId(UUID empresaId);}
