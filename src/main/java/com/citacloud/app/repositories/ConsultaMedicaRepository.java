package com.citacloud.app.repositories; import com.citacloud.app.models.ConsultaMedica; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface ConsultaMedicaRepository extends JpaRepository<ConsultaMedica,UUID>{Optional<ConsultaMedica> findByIdAndEmpresaId(UUID id,UUID empresaId);}
