package com.citacloud.app.repositories; import com.citacloud.app.models.UnidadInventario; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface UnidadInventarioRepository extends JpaRepository<UnidadInventario,UUID>{List<UnidadInventario> findByEmpresaIdAndActivaTrueOrderByNombre(UUID empresaId); Optional<UnidadInventario> findByIdAndEmpresaId(UUID id,UUID empresaId);}
