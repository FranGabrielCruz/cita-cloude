package com.citacloud.app.repositories; import com.citacloud.app.models.ProductoInventario; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface ProductoInventarioRepository extends JpaRepository<ProductoInventario,UUID>{List<ProductoInventario> findByEmpresaIdOrderByNombre(UUID empresaId); Optional<ProductoInventario> findByIdAndEmpresaId(UUID id,UUID empresaId); boolean existsByEmpresaIdAndCodigoIgnoreCase(UUID empresaId,String codigo);}
