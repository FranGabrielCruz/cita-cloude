spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/cita_cloud}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

logging:
  level:
    root: INFO
    com.citacloud.app: DEBUG
    org.hibernate.SQL: DEBUG
